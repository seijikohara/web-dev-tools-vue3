/**
 * Global Setup for E2E Tests
 * Initializes MSW for API mocking
 */

export default async function globalSetup() {
  // MSW setup can be added here if needed for node environment
  // For browser-based mocking, we use playwright test fixtures
}
